# 2026-02-13: Frontend BOT role type and badge component

## 커밋
- `f5ed0b1` feat(frontend): add BOT role type and badge component

## 수정 파일

### `frontend/src/types/models.ts`
- `User.role` 타입에 `'BOT'` 추가 (`'USER' | 'ADMIN' | 'BOT'`)
- `Post.user`, `Comment.user`, `Schedule.user` Pick 타입에 `'role'` 필드 추가

### `frontend/src/components/BotBadge.tsx` (신규)
- 봇 뱃지 컴포넌트 생성
- `size` prop: `'small'` (PostCard/댓글 인라인용) / `'normal'` (프로필 페이지용)
- 보라색 배경 (`#5856D6`), 흰색 'BOT' 텍스트

### `frontend/src/components/PostCard.tsx`
- BotBadge import 추가
- 닉네임 옆에 `post.user.role === 'BOT'` 조건부 뱃지 렌더링

### `frontend/app/[postId]/index.tsx`
- 게시글 상세 헤더에 BOT 뱃지 추가
- 댓글 닉네임 옆에 BOT 뱃지 추가
- 대댓글(reply) 닉네임 옆에 BOT 뱃지 추가
- `nicknameRow` 스타일 추가 (flexDirection: row, alignItems: center, gap: 6)

### `frontend/app/user/[userId].tsx`
- 타 유저 프로필 페이지에 BOT 뱃지 추가 (ADMIN 뱃지와 동일한 `badgeRow` 위치)

### `frontend/app/(tabs)/profile.tsx`
- 내 프로필 페이지에 BOT 뱃지 추가

## 검증
- `npx tsc --noEmit` — 에러 없음
